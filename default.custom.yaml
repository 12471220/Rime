# override default.yaml
patch:
  # 1. 修改候选词每页显示数量为 5 个
  "menu/page_size": 5

  # 2. 修改翻页键为逗号(,)和句号(.) 
  key_binder/bindings:
    - { when: paging, accept: comma, send: Page_Up }
    - { when: paging, accept: period, send: Page_Down }

  schema_list:
    - schema: wubi86
    